This file tells Python:

"These are the external packages my project needs."

For example, later we may need:
fastapi
uvicorn
sqlalchemy
...

Instead of someone manually figuring out what packages to install, they can run:
pip install -r requirements.txt

and install the project's dependencies.



Our Code
   ↓
External Packages
   ↓
Python Environment